<Window x:Class="VNotch.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="V-Notch Setup"
        Width="680"
        Height="520"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <!-- Apple-like easing -->
        <KeySpline x:Key="AppleEase">0.16,1 0.3,1</KeySpline>
        
        <!-- Storyboard for window entrance -->
        <Storyboard x:Key="WindowEntranceStoryboard">
            <!-- Window scale and opacity -->
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                           From="0.96" To="1.0"
                           Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                           From="0.96" To="1.0"
                           Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           Duration="0:0:0.6">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
            <!-- Icon reveal -->
            <DoubleAnimation Storyboard.TargetName="IconContainer"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           BeginTime="0:0:0"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="IconTranslate"
                           Storyboard.TargetProperty="Y"
                           From="8" To="0"
                           BeginTime="0:0:0"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
            <!-- Headline reveal -->
            <DoubleAnimation Storyboard.TargetName="HeadlineText"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           BeginTime="0:0:0.08"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="HeadlineTranslate"
                           Storyboard.TargetProperty="Y"
                           From="8" To="0"
                           BeginTime="0:0:0.08"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
            <!-- Body text reveal -->
            <DoubleAnimation Storyboard.TargetName="BodyText"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           BeginTime="0:0:0.16"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="BodyTranslate"
                           Storyboard.TargetProperty="Y"
                           From="8" To="0"
                           BeginTime="0:0:0.16"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
            <!-- Buttons reveal -->
            <DoubleAnimation Storyboard.TargetName="ButtonPanel"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           BeginTime="0:0:0.24"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ButtonTranslate"
                           Storyboard.TargetProperty="Y"
                           From="8" To="0"
                           BeginTime="0:0:0.24"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="6"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- Page transition storyboard -->
        <Storyboard x:Key="PageTransitionStoryboard">
            <DoubleAnimation Storyboard.TargetName="ContentPresenter"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="4"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- Button hover style -->
        <Style x:Key="SetupButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="SF Pro Text, Segoe UI, Inter, Roboto, Sans-serif"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                          Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#E5E5E5"
                                                          Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                          Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#FFFFFF"
                                                          Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Secondary button style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontFamily" Value="SF Pro Text, Segoe UI, Inter, Roboto, Sans-serif"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                          Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#2A2A2A"
                                                          Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border"
                                                          Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                          To="#1A1A1A"
                                                          Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0F0F0F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Border x:Name="MainBorder"
                CornerRadius="24"
                Background="#0A0A0C"
                BorderBrush="Transparent"
                BorderThickness="0"
                RenderTransformOrigin="0.5,0.5"
                Opacity="0"
                ClipToBounds="True">
            <Border.RenderTransform>
                <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="48" Opacity="0.85" ShadowDepth="0" Color="Black"/>
            </Border.Effect>
            
            <Grid Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Inner border for subtle outline -->
                <Border Grid.RowSpan="3" 
                        CornerRadius="24" 
                        BorderBrush="#1AFFFFFF" 
                        BorderThickness="1"
                        IsHitTestVisible="False"/>
                
                <!-- Title bar -->
                <Border Grid.Row="0" Height="58" Background="#08080A" CornerRadius="23,23,0,0">
                    <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Image Source="Assets/logo.png" Width="30" Height="30" Margin="18,0,0,0"/>
                    
                    <TextBlock Grid.Column="1"
                               Text="Setup Assistant"
                               Foreground="#F2FFFFFF"
                               FontSize="14"
                               FontWeight="SemiBold"
                               FontFamily="SF Pro Text, Segoe UI, Inter, Roboto, Sans-serif"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"/>
                    
                    <Button Grid.Column="2"
                            Width="52"
                            Height="58"
                            Background="Transparent"
                            Foreground="#999999"
                            BorderThickness="0"
                            FontSize="16"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Content>
                            <TextBlock x:Name="CloseIcon" Text="✕" RenderTransformOrigin="0.5,0.5">
                                <TextBlock.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Button.Content>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="Transparent">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="#FF3B30"/>
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(Button.Content).(TextBlock.RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1.15"
                                                                   Duration="0:0:0.15">
                                                        <DoubleAnimation.EasingFunction>
                                                            <ExponentialEase EasingMode="EaseOut" Exponent="4"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                    <DoubleAnimation Storyboard.TargetProperty="(Button.Content).(TextBlock.RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1.15"
                                                                   Duration="0:0:0.15">
                                                        <DoubleAnimation.EasingFunction>
                                                            <ExponentialEase EasingMode="EaseOut" Exponent="4"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(Button.Content).(TextBlock.RenderTransform).(ScaleTransform.ScaleX)"
                                                                   To="1"
                                                                   Duration="0:0:0.15">
                                                        <DoubleAnimation.EasingFunction>
                                                            <ExponentialEase EasingMode="EaseOut" Exponent="4"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                    <DoubleAnimation Storyboard.TargetProperty="(Button.Content).(TextBlock.RenderTransform).(ScaleTransform.ScaleY)"
                                                                   To="1"
                                                                   Duration="0:0:0.15">
                                                        <DoubleAnimation.EasingFunction>
                                                            <ExponentialEase EasingMode="EaseOut" Exponent="4"/>
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Foreground" Value="#FF3B30"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    </Grid>
                </Border>
                
                <!-- Content area -->
                <Border Grid.Row="1" Background="#0A0A0C">
                    <Grid Margin="34,28">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="188"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" CornerRadius="20" Background="#101014" BorderBrush="#18FFFFFF" BorderThickness="1" Padding="18" Margin="0,0,26,0">
                        <StackPanel>
                            <Image Source="Assets/logo.png" Width="86" Height="86" HorizontalAlignment="Left" Margin="0,0,0,22"/>
                            <TextBlock Text="V-Notch" FontSize="22" FontWeight="Bold" Foreground="White" FontFamily="SF Pro Display, Segoe UI Variable Display, Segoe UI, Inter, Roboto, Sans-serif"/>
                            <TextBlock Text="Dynamic Island for Windows" FontSize="12" Foreground="#99FFFFFF" TextWrapping="Wrap" LineHeight="18" Margin="0,6,0,22"/>
                            <StackPanel>
                                <TextBlock x:Name="Step1Text" Text="01  Welcome" Foreground="#FFFFFFFF" FontSize="12" Margin="0,0,0,10"/>
                                <TextBlock x:Name="Step2Text" Text="02  Location" Foreground="#88FFFFFF" FontSize="12" Margin="0,0,0,10"/>
                                <TextBlock x:Name="Step3Text" Text="03  Startup" Foreground="#88FFFFFF" FontSize="12" Margin="0,0,0,10"/>
                                <TextBlock x:Name="Step4Text" Text="04  Install" Foreground="#88FFFFFF" FontSize="12" Margin="0,0,0,10"/>
                                <TextBlock x:Name="Step5Text" Text="05  Finish" Foreground="#88FFFFFF" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Image x:Name="IconContainer" Grid.Row="0" Source="Assets/logo.png" Width="52" Height="52" HorizontalAlignment="Left" Margin="0,0,0,18" Opacity="0">
                            <Image.RenderTransform>
                                <TranslateTransform x:Name="IconTranslate" Y="8"/>
                            </Image.RenderTransform>
                        </Image>
                        
                        <TextBlock x:Name="HeadlineText" Grid.Row="1" Text="Install V-Notch" FontSize="34" FontWeight="Bold" Foreground="White" FontFamily="SF Pro Display, Segoe UI Variable Display, Segoe UI, Inter, Roboto, Sans-serif" Margin="0,0,0,12" Opacity="0">
                            <TextBlock.RenderTransform>
                                <TranslateTransform x:Name="HeadlineTranslate" Y="8"/>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                        
                        <TextBlock x:Name="BodyText" Grid.Row="2" FontSize="14" Foreground="#CCFFFFFF" FontFamily="SF Pro Text, Segoe UI, Inter, Roboto, Sans-serif" LineHeight="23" TextWrapping="Wrap" Opacity="0">
                            <TextBlock.RenderTransform>
                                <TranslateTransform x:Name="BodyTranslate" Y="8"/>
                            </TextBlock.RenderTransform>
                            <Run Text="V-Notch brings a Dynamic Island-inspired experience to Windows."/>
                            <LineBreak/><LineBreak/>
                            <Run Text="It creates a compact surface for media controls, notifications, battery, calendar, and quick actions, all in one place at the top of your desktop."/>
                            <LineBreak/><LineBreak/>
                            <Run Text="Continue to set up V-Notch and make it part of your everyday workflow."/>
                        </TextBlock>
                        
                        <ContentPresenter x:Name="ContentPresenter" Grid.Row="0" Grid.RowSpan="3" Visibility="Collapsed"/>
                    </Grid>
                    </Grid>
                </Border>
                
                <!-- Button panel -->
                <Border Grid.Row="2"
                        Background="#08080A"
                        BorderBrush="#22FFFFFF"
                        BorderThickness="0,1,0,0"
                        CornerRadius="0,0,24,24"
                        Padding="24,16">
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="ButtonTranslate" Y="8"/>
                    </Border.RenderTransform>
                    <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right" Opacity="0">
                        <Button x:Name="CancelButton"
                                Content="Cancel"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Click="CancelButton_Click"/>
                        <Button x:Name="BackButton"
                                Content="Back"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Click="BackButton_Click"
                                Visibility="Collapsed"/>
                        <Button x:Name="NextButton"
                                Content="Continue"
                                Style="{StaticResource SetupButtonStyle}"
                                Click="NextButton_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
