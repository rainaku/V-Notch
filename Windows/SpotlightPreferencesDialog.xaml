<Window x:Class="VNotch.Windows.SpotlightPreferencesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520" SizeToContent="Height" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True"
        WindowStartupLocation="CenterOwner" Loaded="Window_Loaded"
        FontFamily="pack://application:,,,/V-Notch;component/Fonts/#SF Pro Display, Nirmala UI, Segoe UI"
        Foreground="#F2F2F2" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">
    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Background" Value="#141414"/>
            <Setter Property="Foreground" Value="#D8DEE5"/>
            <Setter Property="BorderBrush" Value="#2B2B2B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" CornerRadius="12" Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.65"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Surface" Property="BorderBrush" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="EditorStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#141414"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="SelectionBrush" Value="#555555"/>
            <Setter Property="SelectionOpacity" Value="0.8"/>
            <Setter Property="BorderBrush" Value="#303030"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Surface" CornerRadius="12" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="BorderBrush" Value="#505050"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Surface" Property="BorderBrush" Value="#B8B8B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border x:Name="Shell" Margin="16" CornerRadius="24" Background="#050505"
            BorderBrush="#24FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="4" Opacity="0.35" Color="Black" RenderingBias="Performance"/>
        </Border.Effect>
        <Border.RenderTransform><TranslateTransform x:Name="EntranceOffset"/></Border.RenderTransform>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Margin="24,22,20,18" Background="Transparent" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                    <TextBlock Text="SPOTLIGHT" FontSize="10" FontWeight="SemiBold" Foreground="#909090" Margin="0,0,0,7"/>
                    <TextBlock x:Name="Heading" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </StackPanel>
                <Button x:Name="CloseButton" Grid.Column="1" Style="{StaticResource DialogButton}" Click="Close_Click"
                        MinWidth="32" MinHeight="32" Width="32" Height="32" Padding="0" Background="Transparent"
                        BorderBrush="Transparent" VerticalAlignment="Top">
                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="#B8B8B8"/>
                </Button>
            </Grid>
            <StackPanel Grid.Row="1" Margin="24,0,24,24">
                <Border x:Name="AppCard" Background="#101010" BorderBrush="#1AFFFFFF" BorderThickness="1"
                        CornerRadius="14" Padding="14" Margin="0,0,0,18" Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Grid Width="28" Height="28" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <Image x:Name="AppIcon" Stretch="Uniform"/>
                            <TextBlock x:Name="AppGlyph" Text="&#xE71D;" FontFamily="Segoe MDL2 Assets" FontSize="22"
                                       Foreground="#A0A0A0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="AppTitle" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="AppKind" FontWeight="Normal" FontSize="12" Foreground="#909090" Margin="0,3,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <TextBlock x:Name="Hint" FontWeight="Normal" FontSize="13" Foreground="#A0A0A0" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,12"/>
                <TextBox x:Name="Editor" Style="{StaticResource EditorStyle}" TextChanged="Editor_TextChanged"/>
                <TextBlock x:Name="ErrorText" FontWeight="Normal" FontSize="12" Foreground="#F28B82" TextWrapping="Wrap"
                           Margin="0,10,0,0" Visibility="Collapsed" AutomationProperties.LiveSetting="Assertive"/>
            </StackPanel>
            <Border Grid.Row="2" BorderBrush="#1AFFFFFF" BorderThickness="0,1,0,0" Padding="24,16,24,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Style="{StaticResource DialogButton}" IsCancel="True" Margin="0,0,10,0"/>
                    <Button x:Name="SaveButton" Style="{StaticResource DialogButton}" Background="#F2F2F2" Foreground="#101010"
                            BorderBrush="#F2F2F2" Click="Save_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
