<UserControl x:Class="VNotch.Controls.YouTubePlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300">
    <Grid Background="Black">
        <!-- Thumbnail fallback while loading -->
        <Image x:Name="PlayerThumbnail" Stretch="UniformToFill" Opacity="0.5"/>
        
        <!-- The Browser -->
        <cef:ChromiumWebBrowser x:Name="Browser" 
                                 HorizontalAlignment="Stretch" 
                                 VerticalAlignment="Stretch"
                                 Visibility="Hidden"/>
        
        <!-- Loading overlay -->
        <Grid x:Name="LoadingOverlay" Visibility="Visible">
            <ProgressBar IsIndeterminate="True" 
                         Width="100" Height="4" 
                         Foreground="Red" 
                         Background="Transparent" 
                         BorderThickness="0"
                         VerticalAlignment="Center"/>
        </Grid>

        <!-- Overlay for interactions if needed -->
        <Border Background="Transparent" IsHitTestVisible="False"/>
    </Grid>
</UserControl>
